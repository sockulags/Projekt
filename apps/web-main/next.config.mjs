/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    typedRoutes: true,
  },
  transpilePackages: ['@internal/ui', '@internal/utils', '@internal/config'],
};

export default nextConfig;
